<footer class="footer">
  <div class="container footer__inner">
    <div class="footer__brand">
      <a href="/" class="footer__logo">Brand Dept</a>
      <p>
        Moderne Webseiten für Unternehmen, die<br />
        professionell auftreten möchten.
      </p>
    </div>

    <div class="footer__columns">
      <div class="footer__column">
        <p class="footer__label">Kontakt</p>

        <ul class="footer__list">
          <li><a href="mailto:hello@brand-dept.ch">hello@brand-dept.ch</a></li>
          <li><a href="tel:+41792005399">+41 79 200 53 99</a></li>
          <li>8752 Näfels, Schweiz</li>
        </ul>
      </div>

      <div class="footer__column">
        <p class="footer__label">Rechtliches</p>

        <ul class="footer__list">
          <li><a href="/Impressum">Impressum</a></li>
          <li><a href="/Datenschutz">Datenschutz</a></li>
        </ul>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding-block: var(--space-4xl);
    background: var(--color-background);
    border-top: 1px solid rgba(47, 2, 18, 0.08);
  }

  .footer__inner {
    display: flex;
    justify-content: space-between;
    gap: var(--space-4xl);
  }

  .footer__brand {
    max-width: 20rem;
  }

  .footer__logo {
    display: inline-block;
    margin-bottom: var(--space-md);
    color: var(--color-black);
    font-size: var(--text-p-size);
    line-height: 1;
    font-weight: var(--font-weight-bold);
    text-decoration: none;
  }

  .footer__brand p,
  .footer__list {
    color: rgba(47, 2, 18, 0.55);
    font-size: var(--text-p-small-size);
    line-height: var(--text-p-small-lh);
  }

  .footer__columns {
    display: flex;
    gap: var(--space-5xl);
  }

  .footer__label {
    margin-bottom: var(--space-md);
    color: rgba(47, 2, 18, 0.35);
    font-size: 0.7rem;
    line-height: 1;
    font-weight: var(--font-weight-medium);
    text-transform: uppercase;
    letter-spacing: 0.35em;
  }

  .footer__list {
    display: grid;
    gap: var(--space-xs);
    list-style: none;
  }

  .footer__list a {
    color: inherit;
    text-decoration: none;
  }

  .footer__list a:hover {
    color: var(--color-dark-pink);
  }

  @media (max-width: 700px) {
    .footer__inner {
      flex-direction: column;
      gap: var(--space-2xl);
    }

    .footer__columns {
      gap: var(--space-3xl);
    }

    .footer__brand {
      max-width: none;
    }
  }
</style>
