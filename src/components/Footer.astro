<footer class="footer">
  <div class="container footer__inner">
    <div class="footer__brand">
      <a href="/" class="footer__logo">NIORA</a>
      <p>
        Moderne Webseiten für Unternehmen, die<br />
        professionell auftreten möchten.
      </p>
    </div>

    <div class="footer__columns">
      <div class="footer__column">
        <p class="footer__label">Kontakt</p>

        <ul class="footer__list">
          <li><a href="mailto:hello@niora.de">hello@niora.de</a></li>
          <li><a href="tel:+4989123456">+49 89 123 456</a></li>
          <li>München, Deutschland</li>
        </ul>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding-block: var(--space-4xl);
    background: var(--color-background);
    border-top: 1px solid rgba(47, 2, 18, 0.08);
  }

  .footer__inner {
    display: flex;
    justify-content: space-between;
    gap: var(--space-4xl);
  }

  .footer__brand {
    max-width: 20rem;
  }

  .footer__logo {
    display: inline-block;
    margin-bottom: var(--space-md);
    color: var(--color-black);
    font-size: var(--text-p-size);
    line-height: 1;
    font-weight: var(--font-weight-bold);
    text-decoration: none;
  }

  .footer__brand p,
  .footer__list {
    color: rgba(47, 2, 18, 0.55);
    font-size: var(--text-p-small-size);
    line-height: var(--text-p-small-lh);
  }

  .footer__columns {
    display: flex;
    gap: var(--space-5xl);
  }

  .footer__label {
    margin-bottom: var(--space-md);
    color: rgba(47, 2, 18, 0.35);
    font-size: 0.7rem;
    line-height: 1;
    font-weight: var(--font-weight-medium);
    text-transform: uppercase;
    letter-spacing: 0.35em;
  }

  .footer__list {
    display: grid;
    gap: var(--space-xs);
    list-style: none;
  }

  .footer__list a {
    color: inherit;
    text-decoration: none;
  }

  .footer__list a:hover {
    color: var(--color-dark-pink);
  }
</style>
