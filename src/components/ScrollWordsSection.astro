---
interface Props {
  label?: string;
  words: string[];
}

const { label = "Arbeitsweise", words } = Astro.props;
---

<section class="scroll-words">
  <div class="container">
    {label && <p class="scroll-words__label" data-reveal>{label}</p>}

    <div class="scroll-words__list">
      {
        words.map((word) => (
          <h2 class="scroll-words__word" data-reveal="word">
            {word}
          </h2>
        ))
      }
    </div>
  </div>
</section>

<style>
  .scroll-words {
    padding-block: var(--space-5xl);
    background: var(--color-background);
  }

  .scroll-words__label {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-xl);
    color: var(--color-dark-pink);
    font-size: var(--text-label-size);
    line-height: var(--text-label-lh);
    font-weight: var(--font-weight-semibold);
    text-transform: uppercase;
  }

  .scroll-words__label::before {
    content: "";
    width: 1.5rem;
    height: 1px;
    background: var(--color-dark-pink);
  }

  .scroll-words__list {
    display: grid;
    gap: var(--space-lg);
  }
  .scroll-words__word {
    color: rgba(201, 107, 126, 0.22);
    font-size: var(--text-h1-size);
    line-height: var(--text-h1-lh);
    font-weight: var(--font-weight-bold);
  }

  @media (max-width: 900px) {
    .scroll-words__word {
      font-size: var(--text-h2-size);
      line-height: var(--text-h2-lh);
    }
  }
</style>
